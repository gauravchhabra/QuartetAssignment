#Placeholders for Helper functions 'removeExtraSpaces' & 'normalizeAddress'
def removeExtraSpaces(data):
    return data

def normalizeAddress(address):
    normalizeAddress = address
    return normalizedAddress

#Placeholder function for enriching and normalizing data
def getEnrichedAndNormalizedRecord(record):
    #Iterate over all fields of the record
    for field in record:
        removeExtraSpaces(field)
        if field is address:
            normalizeAddress(field)
    return data
